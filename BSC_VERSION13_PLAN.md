# BSC V13 / Basket V4 前端与 API 升级总计划

核对日期：2026-09-08。根据本任务此前已确认的需求及当前代码整理。
本次实现与验证边界详见 `docs/bsc-v13-lifecycle.md`。以下复选框表示代码接入，不代表已完成真实钱包验收。
本文件是整体交付清单；`docs/bsc-v13-routing.md` 只描述其中的上市后交易模块。

## 1. 当前实际状态

| 模块 | 状态 | 代码核对结果 |
| --- | --- | --- |
| The Graph V13 / Basket V4 索引 | 已完成前序开发 | 独立 schema、映射及部署文档已有；用户已更新索引 |
| Server 同步及 V35 迁移 | 已完成前序开发 | 配置、组件、矿池关联、上市状态、交易、回购及 Basket V4 数据同步已有；用户已执行迁移 |
| V13 上市后 BNB 拆单交易 | 代码及专项测试已完成，待上线联调 | API metadata、单次状态 multicall、Worker 本地优化、主池 IPShare subject、执行合约已有；执行合约部署与生产配置仍需完成核验 |
| V13 代币创建 | 代码已接入 | Pump13、三参数创建、指数配置表单、费用和 salt；新增校验测试 |
| 创建后 API 预入库 | 代码及专项测试完成 | receipt 校验、事务写入、索引先到不覆盖、hash 恢复 |
| V13 内盘 / 等待上市 | 代码已接入 | 动态费率、曲线买卖、pending 禁用、上市路由切换 |
| 代币详情 / 生命周期数据 | 代码已接入 | 阶段、成分/矿池、回购、个人分红、持币人列表 |
| 独立 Basket V4 创建及操作 | 代码及兼容专项测试完成 | 默认 V4、保留 V2/V3；真实 ABI 对照、receipt 恢复 |
| 玩法页 LP 质押矿池 | 代码已接入 | LP 授权、质押、解押、领取、储备与个人持仓 |
| 成分 V2 添加 / 移除流动性 | 代码及本地合约测试完成 | 双资产/BNB 加池、税后最低到账、LP 直接到用户、独立质押 |
| 全生命周期及历史版本回归 | 本地专项完成，链上联调待完成 | 本地测试和构建通过；尚未部署新辅助合约或做真实钱包全流程 |

“已开发”“已测试”“已部署”“用户流程已验收”分别记录，不能相互替代。

## 2. 已确认的产品约束

- UI 在 `bsc-version13`、API 在 `bsc` 分支开发（按用户最新要求）。已有合并不代表剩余功能完成。
- 旧代币按自身版本选择原合约、ABI、费用和玩法；不批量覆盖旧地址。RH 保留独立部署配置。
- V13 上市交易：API 提供代币、合约、池子、候选路径和 tick 清单；浏览器只执行一轮当前状态 multicall，不先发现池子，不补取 API 未提供的 tick。
- 本地计算比较扣除费用、税和 gas 后的收益；收益接近优先少池，明显更优则多池；共享池必须按整条执行顺序模拟。执行合约接收并传递主池 IPShare subject。
- V13 创建必须上传交易 hash。API 验证真实链上结果，在索引到达前写入页面所需的基础数据、配置及关联，后续由索引幂等接管。
- 分红待领取金额直接查询合约，不新增用户待领取分红索引。
- 现有质押合约不修改。按现有仅为调用者记账的方式，添加流动性和质押是两个独立操作；页面可连续引导，但不能承诺一笔交易完成两步。

## 3. 开发顺序与验收条件

### P0：统一版本、ABI 和 API 数据模型

- [x] 增加 Pump13、Token13、Basket V4 及 LP 矿池所需的真实 ABI 与部署配置，保留历史版本映射。
- [x] 设计前后端共用的数据契约：创建参数、内盘状态、listing pending、成分及权重、V2 pair、矿池、关联指数。
- [x] API 接入已有 V35 配置/组件表，提供详情与玩法数据；余额、授权、用户待领取奖励等实时数据明确由链上读取。
- [ ] 统一原始整数、精度、价格计价资产、空值及索引延迟语义；不能把成分资产成交额直接当成 BNB 成交额。
- [x] 核对现有 schema 能否覆盖，只有实际缺列才补增量迁移并写明文件；不要把交易模块“不需迁移”扩大为整个项目的结论。

验收：无论索引先到还是 API 先登记，同一代币只有一套一致的版本、配置及池子关联；旧版本读取不变。

### P1：创建到内盘交易的完整流程

- [x] 创建表单增加 1–4 个白名单成分、权重（总计 100%）、指数名称/符号、1%–3% 指数费率、最高 30% 的费用内部创建者分成、社区权限保留选项。
- [x] 提交前校验地址、重复成分、白名单、字节长度及 bps；可用路由由实际 Pump 创建模拟校验；按 V13 实际实现预测地址、搜索 salt、调用三参数 createToken。
- [x] 创建费包含 Pump、可选 IPShare、Community 和“成分数 × settings fee”；初始买入金额与固定费分开显示。
- [x] 交易 hash 按 chain/account 持久化，失败重试只补 API 登记，不能重复发部署交易；切链或换账户不得重放错误草稿。
- [x] API 校验 receipt 成功状态、链、发出事件的 Pump、token/tick/creator，并从真实结果提取配置、成分 Pair、全部矿池、社区 owner 等数据，不信任前端上报地址。
- [x] 在同一数据库事务中完成创建所需预入库；处理重复请求、索引先到、响应丢失、并发登记，避免旧快照覆盖新状态，不推进索引游标。
- [x] 内盘报价、买入、卖出接入 V13 的费用、anti-snipe、IPShare、滑点及授权规则；新币创建完成后能立即进入可用详情及交易页。
- [x] listingPending 时停止内盘交易并显示等待状态；上市成功切换多池交易；失败恢复后能够重新显示内盘，不以单个布尔字段误判。

验收：创建（含/不含初始买入）→ API 立即可见 → 内盘买卖 → 等待上市 → 上市后交易，整个流程可走通。

### P2：详情页和社区数据展示

- [x] 内盘进度、当前阶段、等待上市原因/提示及刷新机制。
- [x] 展示指数信息、成分/目标权重、各 V2 Pair 与 LP 矿池地址、奖励分配比例，以及与自动生成 Basket 的关联。
- [x] 上市后展示主池与成分池的可用数据、流动性及有明确计价单位的价格/交易信息。
- [x] 区分目标权重与实际资产占比，区分尚未注入流动性与加载失败。
- [x] 展示回购相关协议数据；用户待领取分红直接读合约，领取操作按真实 ABI 接入。
- [ ] 桌面/移动端布局、中文/英文、空状态和错误状态一并完成。

验收：无初始流动性、内盘、pending、已上市、有/无持仓等状态都可理解，不显示错误的零值或旧社交矿池信息。

### P3：独立指数升级 Basket V4

- [x] UI/API 新建指数默认 V4，配置真实 Hook、SwapRouter、TokenDeployer、Executor 及相应 ABI；共享组件按真实部署关系复用。
- [x] 创建参数、成分路由校验、创建费/首笔交易及交易 receipt 解析同步升级。
- [x] 创建 hash 交 API，完成可信预入库及幂等恢复；区分独立创建与 Pump 上市自动生成的指数。
- [x] 列表、详情、买卖、调仓、费用领取等已有 Basket 功能按版本派发，验证 V4 新行为与兼容差异。
- [x] 历史 Basket V2/V3 保持可展示、可操作；不得仅替换全局合约地址。

验收：独立 V4 创建及后续操作、Pump 关联 V4 展示、历史指数回归均通过。

### P4：玩法页 LP 矿池和流动性

- [x] 为 BSC V13 增加 LP 矿池玩法入口，展示每个成分资产的 Pair、奖励比例、总质押、个人质押和可领取奖励。
- [x] 接入 LP 授权、质押、解除质押、领取奖励，并正确处理零余额、池关闭和交易刷新。
- [x] 添加流动性：按储备计算配比、授权、滑点与最少 LP，展示实际消耗与剩余资产；计入 V13 成分 Pair 的转账销毁税。
- [x] 移除流动性：LP 授权、最少到账、税后预估与收款账户。
- [x] 在同一页面连续引导“添加流动性 → 收到 LP → 用户直接质押”，保存步骤，第二步失败时允许恢复，已有 LP 可直接质押。
- [x] 对用户只持有 BNB 的一站式加池，评估并实现必要的兑换/加池执行能力；现有交易执行合约仅有 buy/sell，不能默认视为已支持 zap。质押仍独立。
- [x] 与既有 NFT、Basket、AI 等玩法并存，仅对符合条件的代币显示。

验收：从 BNB 或已有资产添加流动性，获得 LP 后质押、领取、解除质押并移除流动性，能完整操作及恢复中断步骤。

### P5：集成回归和交付

- [ ] 核对 Graph/API/链上三种数据到达顺序和最终一致性。
- [ ] 覆盖 V13 完整流程、历史代币和历史 Basket、RH、切链/换账户、拒签/失败/超时重试、滑点及税。
- [x] API 集成测试、前端类型检查/构建、关键合约交互测试及实际页面检查。
- [ ] 验证执行合约部署地址、主池 subject、Basket 所需链上授权与生产配置，不以本地测试替代上线验证。
- [ ] 更新接口/操作/部署文档，列出真实迁移、配置和发布顺序，再按请求提交、合并。

## 4. 完成标准

整体交付要求所有上述模块完成并验证，或明确记录用户取消的项。
仅更新创建版本、仅合并 API 分支、仅通过路由测试，都不满足整体完成标准。
